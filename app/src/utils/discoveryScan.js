/**
 * Discovery Scan — scans drawings to build a discovery index of detectable elements.
 * Stub implementation — will be expanded when Discovery feature is built.
 */

/** Abort any in-flight scan and re-scan the full drawing set. */
export async function rescanDrawings(_drawings) {
  // No-op stub — discovery scanning not yet implemented
}
